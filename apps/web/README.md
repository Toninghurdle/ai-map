# apps/web

Run `pnpm install` at the repo root, then `pnpm dev` (or `pnpm --filter web dev` from here) and open `http://localhost:3000`.

Copy the root `.env.example` to `.env.local` in this folder before running locally; the defaults (`DATA_SOURCE=static`) work with no database.
