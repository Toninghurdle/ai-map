# Copy to apps/web/.env.local for local dev, and set the same names in Vercel.
# Public values (safe to commit; designed to be exposed to the browser).
NEXT_PUBLIC_SUPABASE_URL=https://uxlujfmekclqvigbohry.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_JgKLnIOmr3cDb5Q10c0n4A_Ul2V8W-J
NEXT_PUBLIC_SITE_URL=https://ai-map.dominic-deane.com

# Where the site reads data from: "static" (data/v2 files, works with no database) or "supabase".
DATA_SOURCE=static

# Secrets: never commit. Local .env.local and Vercel environment settings only.
SUPABASE_SECRET_KEY=
SUPABASE_DB_PASSWORD=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
RESEND_API_KEY=
